#include "base_monster.h"
#include "base_skill.h"

/*
void base_monster::hited_slot(base_monster* opponent, base_skill* used, int hurt){
    if(hurt >= this->hp.real){
        emit this->died(opponent);
    }
    else{
        this->hp.real -= hurt;
        if(used->isDirect()) emit this->touched(opponent);
    }

}
*/

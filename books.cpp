#include "books.h"

int books::monster_id   = 0;
QMap<size_t, QString> books::monsters_map   = QMap<size_t, QString>();
int books::ability_id   = 0;
QMap<size_t, QString> books::abilities_map  = QMap<size_t, QString>();
int books::skill_id     = 0;
QMap<size_t, QString> books::skills_map     = QMap<size_t, QString>();
